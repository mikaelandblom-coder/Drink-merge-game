{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "game",
      "autoPort": true,
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; socketserver.TCPServer(('',int(os.environ.get('PORT',5500))),http.server.SimpleHTTPRequestHandler).serve_forever()"],
      "port": 5500
    }
  ]
}
